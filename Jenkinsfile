pipeline {
    agent any
    stages {
        stage('Building Docker Image') {
            steps {
                sh 'sudo docker build -t sainathgaripally/project1 .'
            }
        }
    }
}
