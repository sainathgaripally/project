pipeline {
    agent any
    stages {
        stage('Building Docker Image') {
            steps {
              script {
                sh 'docker version'
      	      }
            }
        }
    }
}
